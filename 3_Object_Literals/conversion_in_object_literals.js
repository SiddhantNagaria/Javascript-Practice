const obj = {
    1:"a",
    2:"b",
    null:"c",
    undefined:"d",
    true:"e"
};

console.log(obj);

//keys are converted to strings automatically
// even if key is a Number, then also converted into string 